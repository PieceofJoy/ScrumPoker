import React from 'react';
import { Link } from 'react-router-dom';

const Room = () => {
    return (
        <div>
            Hello Room
            <Link to="/">Go to Main</Link>
        </div>
    );
};

export default Room;